// Given a grocery item list calculate the total price the customer will have to pay to buy all the 
//items.

function groceryPrice(groceries) {
    let total = 0;
    for(let keys in groceries){
        total += 
    }
}

let groceries = {
    banana : 12,
    Milk : 10.5,
    Bread : 8,
    Egg: 2,
};

let price = groceryPrice(groceries);
console.log(`Total price for grocery item is ${price}`)
// Tip: You will need to iterate through all the keys of given groceries object. You can read about 
//for in loop and try completing the code.