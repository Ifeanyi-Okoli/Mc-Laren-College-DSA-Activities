// Given a string, check if it is palindromic or not using recursion.
function reverse(str, char,){
    let arr = str.split("");
    let arr1 = arr.reverse();
    let str1 = arr1.join("");
    if()
}

function isPalindromic(str) {
  let firstChar = str[0];
  let remaining = str.substring(1);
if(reverse(str, firstChar)){

}
}

console.log(isPalindromic('viv'));//true
console.log(isPalindromic('abc'));//false
console.log(isPalindromic(''));//true